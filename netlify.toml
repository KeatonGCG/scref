# Sandcastle Fitness — One‑Page Referral Site
[build]
  publish = "."
  functions = "netlify/functions"

# /r/CODE -> server-side tracking -> sets cookies -> 302 to /
[[redirects]]
from = "/r/:code"
to = "/.netlify/functions/r?code=:code"
status = 200
force = true
